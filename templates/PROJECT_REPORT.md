# Project Report

> What is this project, right now? Rewrite this file as things change —
> it should always describe the *current* state, not a history of how it
> got here (git log and `DEVELOPMENT_MEMORY.md`'s decision log are for
> that). See `SKILL.md` for when to update this file.

## Current Objective

What is this project trying to accomplish right now? Not the original
pitch — the actual current goal, which may have narrowed or shifted
since the project started.

## Architecture

Brief. Link to real docs/code if a fuller description exists elsewhere —
don't duplicate a design doc here.

## Current State / Release

What's actually working right now? What's deployed, what's merged, what
exists only locally.

## Known Constraints

Real limits — technical, organizational, or otherwise — that any agent
picking this up needs to respect without being told twice.

## Verified Facts

Things that have actually been checked, not just assumed. If it hasn't
been verified, it belongs in `DEVELOPMENT_MEMORY.md` as an assumption,
not here.

## Open Risks

What could go wrong that hasn't yet? Not a wishlist of hypothetical
concerns — only risks worth a future agent actually watching for.
